# --- Zerodha (fill before LIVE) ---
KITE_API_KEY=
KITE_API_SECRET=
KITE_ACCESS_TOKEN=

# --- Mode & symbols ---
MODE=paper               # paper | live
SYMBOLS=TATAMOTORS,LT,SBIN
BAR_SECONDS=10
ENTRY_EPS_BPS=5         # epsilon buffer on entry (bps). 20 in paper to see fills; 0–1 in live.

# --- Risk & entry ---
RISK_RS=1000             # default rupee risk per trade (editable in UI)
ENTRY_MODE=6TO10         # default; UI can override
SLIPPAGE_CAP_PCT=0.0005  # 0.05%
FILL_TIMEOUT_MS=500

# --- Paths (auto-created) ---
DATA_DIR=./data
