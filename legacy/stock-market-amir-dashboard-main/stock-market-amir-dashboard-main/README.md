# ProbEdge Terminal (Structure Engine + Live Tracker + Journal)

**Tech**: Python 3.9+, Streamlit ≥1.50,<2, Plotly 5.x  
**Status**: Modularized; Journal + Terminal working; Live ticker fix planned (websocket auto-push).

---

## 1) Quick Start (Local / Codespaces)

### A. Clone & env
```bash
git clone <your-repo-url>
cd stock-market-amir-dashboard

# Create local .env from template and fill in (do NOT commit .env)
cp .env.example .env